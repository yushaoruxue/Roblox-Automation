# Python / runtime artifacts
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# Generated debug/runtime output (not source)
diagnostics/
automation.log
*.log
*.tmp

# Agent workspace data (not project source)
.workbuddy/

# Auto-generated backup files
*.bak

# Experimental lab (research scripts + captured frame evidence), kept out of
# the AE production baseline to avoid committing multi-MB frame captures.
experiments/

# Generic scripts + captured templates are user runtime data (created via the
# GUI), not source code; kept out of the repository like experiments/.
scripts/

# Unrelated empty placeholder (0 bytes) - not part of the project, kept out
# of the public repository. Local file is preserved.
clash_proxies.json

# IDE / editor local config (no shared value)
.vscode/
.idea/
*.swp
*.swo
